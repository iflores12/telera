create table temperature (
	run_id datetime
	, temperature_celsius
);

create table carbon_dioxide (
	run_id datetime
	, eco2
);

create table humidity (
	run_id datetime
	, relative_humidity
);

create table range (
	run_id datetime
	, range_mm
);
